import React from "react";

import "./querybar.scss";

const Today = (props) => {
  return (
    <form className="query-bar" noValidate>
      <div>
        <label>Today</label>
      </div>
    </form>
  );
};

export default Today;
